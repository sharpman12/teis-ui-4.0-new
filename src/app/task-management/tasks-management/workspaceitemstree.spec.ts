import { Workspaceitemstree } from './workspaceitemstree';

describe('Workspaceitemstree', () => {
  it('should create an instance', () => {
    expect(new Workspaceitemstree()).toBeTruthy();
  });
});
